# Public Assets

Place your Silicon logo image here as:
- `silicon-logo.png` or
- `logo.png`

The logo should be a square image (recommended: 256x256px or 512x512px) with a transparent background.

The header will automatically use this logo if it exists, otherwise it will fall back to the "S" text logo.

